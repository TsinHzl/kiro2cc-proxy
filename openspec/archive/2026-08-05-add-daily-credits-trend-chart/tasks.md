# 任务清单：add-daily-credits-trend-chart

## 状态：ARCHIVED

## 任务
- [x] 新建 `admin-ui/src/components/daily-credits-trend-chart.tsx`：实现最近 14 天 UTC 日历日序列生成（`Date.UTC` 纯整数运算，不经浏览器本地时区）与缺失日期补 0、按日期升序排列、坐标计算
  - spec.md 引用：场景「横轴覆盖最近 14 个自然日」「时间基准与后端日期字段保持一致」
- [x] 组件内实现 SVG 渲染：网格线、渐变面积填充、折线、数据点圆点标记、数值标签、x 轴日期标签、图例、标题
  - spec.md 引用：场景「数据点上方展示数值标签」「图表视觉呈现要素」
- [x] 处理全部数据为 0 / 无数据的边界情况（y 轴网格线退化逻辑，避免除零）
  - spec.md 引用：场景「近 14 天用量全部为 0」
- [x] `daily-stats-page.tsx` 引入并挂载该组件到标题行与表格之间
  - spec.md 引用：场景「打开每日统计页面且存在用量数据」「数据加载中」

## 验收标准
- [ ] 每日统计页面顶部展示折线 + 面积图，横轴为最近 14 个自然日（含今天），纵轴为 credits 用量
- [ ] 图表同时包含：标题、y 轴网格线（带刻度数值）、渐变面积填充、折线、数据点圆点标记、x 轴日期标签、图例文本——与参考截图的视觉构成一致
- [ ] 曲线上每个数据点上方显示对应的 credits 数值
- [ ] 缺失用量记录的日期在图上显示为 0，不中断曲线连续性
- [ ] 在非 UTC+8 时区的浏览器环境下手动验证，横轴日期序列与表格中的日期一一对应，不出现错位一天
- [ ] 不新增任何 npm 依赖，`admin-ui/package.json` 无变化
- [ ] `tsc -b`（`pnpm build`）无类型错误
